// Storing this here for this example for ease.
// Constants that change per environment would instead go in environment specific config in the app or externally as required.
export const BALANCE_SHEET_API_URL: string = 'http://localhost:3001/api/balance-sheet';
